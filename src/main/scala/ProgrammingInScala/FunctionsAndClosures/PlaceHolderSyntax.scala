package ProgrammingInScala.FunctionsAndClosures

object PlaceHolderSyntax extends App {
  val someNumbers = List(-11, -10, -5, 0, 5, 10)
  someNumbers.filter(_ > 0) // All the numbers that are greater than 0
}
